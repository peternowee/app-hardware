version: '3.4'

services:
  identifier:
    image: semtech/mu-identifier:1.9.1
    environment:
      SESSION_COOKIE_SECURE: "on"
    links:
      - dispatcher:dispatcher
    ports:
      - "80:80"
  dispatcher:
    image: semtech/mu-dispatcher:2.1.0-beta.2
    links:
      - hardware:hardware
    volumes:
      - ./config/dispatcher:/config
  database:
    image: redpencil/virtuoso@sha256:5693311663f14e08dc536f6e6b4d4e6aab70ce3f1b68afcdd26efe30b977f48c
    environment:
      SPARQL_UPDATE: "true"
      DEFAULT_GRAPH: "http://mu.semte.ch/examples/hardware"
    ports:
      - "8890:8890"
    volumes:
      - ./data/db:/data
      - ./config/virtuoso/virtuoso.ini:/data/virtuoso.ini
  hardware:
    image: semtech/mu-cl-resources:1.22.2
    environment:
      MU_APPLICATION_GRAPH: "http://mu.semte.ch/examples/hardware"
    links:
      - database:database
    volumes:
      - ./config/resources:/config
